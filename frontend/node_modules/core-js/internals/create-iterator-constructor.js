'use strict';
var IteratorPrototype = require('core-js/internals/iterators-core').IteratorPrototype;
var create = require('core-js/internals/object-create');
var createPropertyDescriptor = require('core-js/internals/create-property-descriptor');
var setToStringTag = require('core-js/internals/set-to-string-tag');
var Iterators = require('core-js/internals/iterators');

var returnThis = function () { return this; };

module.exports = function (IteratorConstructor, NAME, next) {
  var TO_STRING_TAG = NAME + ' Iterator';
  IteratorConstructor.prototype = create(IteratorPrototype, { next: createPropertyDescriptor(1, next) });
  setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
  Iterators[TO_STRING_TAG] = returnThis;
  return IteratorConstructor;
};
