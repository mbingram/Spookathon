'use strict';
var toPrimitive = require('core-js/internals/to-primitive');
var definePropertyModule = require('core-js/internals/object-define-property');
var createPropertyDescriptor = require('core-js/internals/create-property-descriptor');

module.exports = function (object, key, value) {
  var propertyKey = toPrimitive(key);
  if (propertyKey in object) definePropertyModule.f(object, propertyKey, createPropertyDescriptor(0, value));
  else object[propertyKey] = value;
};
