var DESCRIPTORS = require('core-js/internals/descriptors');
var propertyIsEnumerableModule = require('core-js/internals/object-property-is-enumerable');
var createPropertyDescriptor = require('core-js/internals/create-property-descriptor');
var toIndexedObject = require('core-js/internals/to-indexed-object');
var toPrimitive = require('core-js/internals/to-primitive');
var has = require('core-js/internals/has');
var IE8_DOM_DEFINE = require('core-js/internals/ie8-dom-define');

var nativeGetOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? nativeGetOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
  O = toIndexedObject(O);
  P = toPrimitive(P, true);
  if (IE8_DOM_DEFINE) try {
    return nativeGetOwnPropertyDescriptor(O, P);
  } catch (error) { /* empty */ }
  if (has(O, P)) return createPropertyDescriptor(!propertyIsEnumerableModule.f.call(O, P), O[P]);
};
