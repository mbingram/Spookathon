var getBuiltIn = require('core-js/internals/get-built-in');
var getOwnPropertyNamesModule = require('core-js/internals/object-get-own-property-names');
var getOwnPropertySymbolsModule = require('core-js/internals/object-get-own-property-symbols');
var anObject = require('core-js/internals/an-object');

// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
  var keys = getOwnPropertyNamesModule.f(anObject(it));
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  return getOwnPropertySymbols ? keys.concat(getOwnPropertySymbols(it)) : keys;
};
