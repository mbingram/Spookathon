'use strict';
var $ = require('core-js/internals/export');
var DESCRIPTORS = require('core-js/internals/descriptors');
var FORCED = require('core-js/internals/object-prototype-accessors-forced');
var toObject = require('core-js/internals/to-object');
var aFunction = require('core-js/internals/a-function');
var definePropertyModule = require('core-js/internals/object-define-property');

// `Object.prototype.__defineGetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__defineGetter__
if (DESCRIPTORS) {
  $({ target: 'Object', proto: true, forced: FORCED }, {
    __defineGetter__: function __defineGetter__(P, getter) {
      definePropertyModule.f(toObject(this), P, { get: aFunction(getter), enumerable: true, configurable: true });
    }
  });
}
