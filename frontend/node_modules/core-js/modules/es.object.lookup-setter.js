'use strict';
var $ = require('core-js/internals/export');
var DESCRIPTORS = require('core-js/internals/descriptors');
var FORCED = require('core-js/internals/object-prototype-accessors-forced');
var toObject = require('core-js/internals/to-object');
var toPrimitive = require('core-js/internals/to-primitive');
var getPrototypeOf = require('core-js/internals/object-get-prototype-of');
var getOwnPropertyDescriptor = require('core-js/internals/object-get-own-property-descriptor').f;

// `Object.prototype.__lookupSetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__lookupSetter__
if (DESCRIPTORS) {
  $({ target: 'Object', proto: true, forced: FORCED }, {
    __lookupSetter__: function __lookupSetter__(P) {
      var O = toObject(this);
      var key = toPrimitive(P, true);
      var desc;
      do {
        if (desc = getOwnPropertyDescriptor(O, key)) return desc.set;
      } while (O = getPrototypeOf(O));
    }
  });
}
